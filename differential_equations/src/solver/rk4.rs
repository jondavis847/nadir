use crate::MultibodySystem;
use super::{Solver,SolverTrait};

pub struct Rk4 {
    solver: Solver
}

pub enum Rk4Stages

impl SolverTrait for Rk4 {
    
    fn solve(&mut self, sys: &mut MultibodySystem) {
        

        for joint in &mut sys.joints {

        }
    }
}