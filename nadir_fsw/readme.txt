nadir fsw consists of 4 apps:

sensor_processing -> state_determination -> state_control -> actuator_processing

the names are chosen to be similar to the GSFC ACS App names DI -> AD/OD -> AC -> DO

Note: where does OM fall? I'm sure we'll figure it out as we code